name: release

on:
  push:
    tags:
      - 'v*.*.*'
    workflow_dispatch:

permissions:
  contents: write

jobs:
  create_release:
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Compare Version
        run: |
          CURRENT=v$(jq -r '.version' info.json)
          LATEST=$(git tag | grep -E '^v[0-9]+\.[0-9]+\.[0-9]+' | tail -1)
          if [[ "$CURRENT" < "$LATEST" ]]; then
            echo "ERROR: current manifest version $CURRENT is lower than latest version $LATEST"
            exit 1
          fi
          echo "Current version $CURRENT >= $LATEST (latest), continuing to build"
      - name: Githu release
        run: |
          VERSION=$(jq -r '.version' info.json)
          NOTES=$(git log -1 --format='## Changes:%n%b')
          gh release create "v$VERSION" --notes "$NOTES"

  # deploy_mod_portal:
  #   needs: create_release
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Create link
  #       run: |
  #         MODS_API="https://mods.factorio.com/api/v2/mods/init_publish"
  #         MOD_NAME=$(jq -r '.name' info.json)
  #         HEADERS="Bearer ${{ secrets.FACTORIO_KEY }}"


